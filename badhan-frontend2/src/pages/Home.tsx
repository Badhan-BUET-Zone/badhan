import React from 'react';
import { Typography } from '@mui/material';

const Home: React.FC = () => {
  return <Typography variant="h5">Welcome to the Home page.</Typography>;
};

export default Home;
