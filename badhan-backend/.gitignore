node_modules/
playground/
dotenv/config.json
.idea/
app.yml
env.production
env.development
dist

# The designer's certificate artwork, baked into one background per viewer: with the
# signature block for a signed-in viewer, without it for the public verification page.
# Like the env files above, both live in the private secrets repo and are fetched into
# place for a deploy, not committed — see upload-gcloud.js and
# scripts/certificate-assets/render-background.sh.
src/assets/certificate-background.png
src/assets/certificate-background-public.png

.vscode
.DS_Store
src/.DS_Store
last_deployed.txt
logs
mongodb_local
backup
config

# TSOA generated files
src/tsoaRoutes/
dist/tsoa/
