# Local environment.
# MONGODB_URI is used by the backup/restore internal server as the default
# RESTORE target for POST /restore/:date (when no development flag is passed).
MONGODB_URI=mongodb://mongo:27017/Badhan
JWT_SECRET=badhandev
VUE_APP_FRONTEND_BASE=http://localhost:8080
RATE_LIMITER_ENABLE=false
